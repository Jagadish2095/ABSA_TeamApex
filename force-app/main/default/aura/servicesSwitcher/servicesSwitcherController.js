({
    doInit : function(component, event, helper) {
        helper.handleInit(component, event);
    },

    startBioFlow : function(component, event, helper) {
        helper.handlestartBioFlow(component, event);
    }
})