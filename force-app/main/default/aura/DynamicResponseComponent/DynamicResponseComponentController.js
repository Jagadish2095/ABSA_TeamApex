({
    doInit : function(component, event, helper) {
      //  helper.doInit(component, event, helper);
        helper.getList(component, event, helper);
        helper.getResponseList(component, event, helper);
    }
})