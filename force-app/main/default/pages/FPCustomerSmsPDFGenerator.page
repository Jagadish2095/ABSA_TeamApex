<apex:page controller="FPCustomerSmsDataCtrl" renderAs="pdf">
    {!PDFData}
    <!--  {!jsonstring[0].body[0].timeCaptured}
    <apex:form>
        <apex:pageBlock>
            <apex:pageBlockTable value="{!jsonstring[0].body[0]}" var="res">
                <apex:column headerValue="Time Captured" value="{!res.timeCaptured}" />
                <apex:column headerValue="Comments" value="{!res.comments.commentText}" />
            </apex:pageBlockTable>
        </apex:pageBlock>

    </apex:form> -->
</apex:page>