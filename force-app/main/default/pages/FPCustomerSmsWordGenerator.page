<apex:page controller="FPCustomerSmsDataCtrl" contentType="application/vnd.msword">
    {!PDFData}
    <!-- {!jsonstring[0].body[0].timeCaptured}
     <apex:repeat value="{!jsonstring.body}" var="time">
        <apex:outputText value="{!time.timeCaptured}" />
        <br/>
        <apex:repeat value="{!time.comments}" var="com">
            <apex:outputText value="{!com.commentText}" />
            <br/>
        </apex:repeat>
    </apex:repeat>
 -->

</apex:page>